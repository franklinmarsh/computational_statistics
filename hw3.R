GenX0 <- function(m) {
  return(c(1:m))
}

GenSwaps <- function(x0) {
  
  out <- list() #initialize empty list to hold all possible swaps
  
  for (swap in x0) {
    
  }
  
}

FindSize <- function(x) {
  
  sizes <- list()
  
  for (j in (0:x) {
      sizes[i] <- x[i]*i
  }
  
  return(sum(sizes))
    
}